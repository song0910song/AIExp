# 从零启动教程

本文档面向**从未接触过本项目的开发者**：按顺序执行即可在本机跑起室内照明设计智能体的 CLI、测试与 Web 工作台。所有命令以 Windows 11 + PowerShell 为例（其他系统请自行替换对应命令）。

---

## 0. 项目是什么

可审计的室内照明设计智能体：LLM 只负责理解需求与编排工具，项目事实、计算、规则校核、候选灯具均保存为可追溯数据。

- **后端**：Python ≥ 3.14，FastAPI + LangChain + Chroma（`src/lighting_agent/`）
- **前端**：Next.js 16 工作台（`web/`）
- **数据**：SQLite（`data/lighting_design.sqlite3`）+ 项目快照 + 模型缓存（`.model-cache/`）
- **外部依赖**：DIALux Luminaire Finder（灯具目录，需联网）、LLM 网关（仅智能对话需要）

---

## 1. 前置条件

| 工具 | 版本要求 | 用途 |
|---|---|---|
| [uv](https://docs.astral.sh/uv/) | 最新版 | Python 依赖与虚拟环境管理（必需） |
| Python | ≥ 3.14 | 后端运行时（uv 可代为安装） |
| [Node.js](https://nodejs.org/) | ≥ 20（推荐 LTS 22） | 前端 dev 服务器 |
| 网络 | — | 安装依赖、DIALux 目录、LLM 网关、首次下载嵌入模型 |

### 1.1 安装 uv（含 Python）

```powershell
# 安装 uv（官方一键脚本）
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
uv --version

# 由 uv 安装并托管 Python 3.14（无需手动安装 Python）
uv python install 3.14
```

> 若已安装其他版本管理工具（conda / pyenv），只要能让 `python --version` 输出 ≥ 3.14 即可，uv 会自动识别。

### 1.2 安装 Node.js

从 <https://nodejs.org/> 下载 LTS 版（≥ 20）安装，验证：

```powershell
node --version   # 应 ≥ v20
npm --version
```

---

## 2. 获取代码并安装依赖
要先下载git，网址：https://git-scm.com/install/windows

```powershell
# 克隆仓库（或直接解压代码压缩包到任意目录）
git clone https://github.com/song0910song/AIExp.git lighting-agent
cd lighting-agent

# 安装 Python 依赖（含 dev 组 pytest）
uv sync --group dev
```

> ⚠️ 依赖包含 `torch`、`chromadb`、`sentence-transformers`，体积较大，首次安装需几分钟，请耐心等待。
> 该命令会自动创建 `.venv` 虚拟环境，无需手动激活——之后一律用 `uv run` 前缀执行 Python。

---

## 3. 首次下载嵌入模型（RAG 检索需要）

RAG 默认使用 `BAAI/bge-small-zh-v1.5` 嵌入模型，且默认只从本地缓存加载（`local_files_only=true`）。首次使用前需下载一次：

```powershell
# 国内直连 huggingface.co 常超时（WinError 10060），必须走镜像
$env:HF_ENDPOINT = "https://hf-mirror.com"
$env:LIGHTING_EMBEDDING_LOCAL_FILES_ONLY = "false"

uv run python -c "from lighting_agent.rag import create_evidence_store; create_evidence_store()"
```

模型会缓存到项目根目录 `.model-cache/`。下载完成后即可恢复默认离线模式（重开终端环境变量自动失效）。

> 若只做计算/图纸解析等不涉及检索的操作，可跳过本步；也可用 `$env:LIGHTING_RAG_BACKEND = "local"` 临时切到 SQLite 关键词检索，完全不依赖嵌入模型。

---

## 4. 验证安装

```powershell
# CLI 可用性
uv run python main.py --help

# 运行测试（Windows 必须加 --basetemp，否则系统临时目录权限报错）
uv run pytest tests/ -q --basetemp .pytest-basetemp
```

全部测试通过即说明环境就绪。

---

## 5. 环境变量（可选）

| 变量 | 默认值 | 说明 |
|---|---|---|
| `LIGHTING_LLM_API_KEY` | 内置默认值 | 仅 `chat` 命令与 Web 智能对话需要；离线命令（建项目、计算、检索、选型）不需要 |
| `LIGHTING_LLM_MODEL` | `deepseek-v4-flash` | 对话模型 |
| `LIGHTING_LLM_BASE_URL` | `https://opencode.ai/zen/go/v1/` | LLM 网关 |
| `LIGHTING_RAG_BACKEND` | `chroma` | `local` = SQLite 关键词检索（无需嵌入模型） |
| `DIALUX_BASE_URL` | `https://luminaires.dialux.com` | DIALux 目录站点 |
| `LIGHTING_EMBEDDING_CACHE_FOLDER` | `.model-cache` | 嵌入模型缓存目录 |

完整清单见 [README.md](../README.md) 与 `src/lighting_agent/config.py`。设置方式示例：

```powershell
$env:LIGHTING_LLM_API_KEY = "你的密钥"   # 仅 chat 需要
$env:LIGHTING_LLM_MODEL = "deepseek-v4-flash"
```

---

<<<<<<< HEAD
=======

>>>>>>> 81f377461e2a931ed1c5312c8d2282904eee70b6
## 7. 启动 Web 工作台

需要**两个终端**：

**终端 A —— 后端**（项目根目录）：

```powershell
uv run uvicorn lighting_agent.web_api:app --reload
```

- API 地址：<http://127.0.0.1:8000>
- 交互式 API 文档：<http://127.0.0.1:8000/docs>
- 健康检查：`GET /api/health`

**终端 B —— 前端**：

```powershell
cd web
npm install     # 首次执行
npm run dev
```

浏览器打开 <http://localhost:3000>。前端所有 `/backend/*` 请求由 Next.js 自动代理到后端 `/api/*`，无需额外配置；CORS 白名单已内置 `localhost:3000`。

> 后端端口被占用时：`uv run uvicorn lighting_agent.web_api:app --reload --port 8001`，并在前端终端设置 `$env:LIGHTING_API_URL = "http://127.0.0.1:8001"` 后重启 `npm run dev`。

---

## 8. 常见问题排查

| 现象 | 原因与解法 |
|---|---|
| `WinError 10060`（连接 huggingface.co 超时） | 走镜像：先 `$env:HF_ENDPOINT = "https://hf-mirror.com"`、`$env:LIGHTING_EMBEDDING_LOCAL_FILES_ONLY = "false"` 再下载模型（见第 3 节） |
| pytest 报系统临时目录权限错误 | Windows 下必须加 `--basetemp .pytest-basetemp` |
| chat 报 `LIGHTING_LLM_API_KEY is required` | 该部署未内置默认密钥，需自行设置 `LIGHTING_LLM_API_KEY`；离线命令不受影响 |
| 检索报嵌入模型相关错误 | 模型未下载到 `.model-cache`，执行第 3 节；或临时 `$env:LIGHTING_RAG_BACKEND = "local"` |
| DIALux 搜索超时/失败 | 检查外网连通性；站点需可直连 `https://luminaires.dialux.com`，可调 `DIALUX_TIMEOUT_SECONDS` |
| 上传 `.dwg` 图纸失败 | `.dxf` 原生支持；`.dwg` 需本机安装 ODA File Converter 才能转换 |
| 前端打不开或接口 404 | 确认后端已启动且监听 8000；Next 16 需 Node ≥ 20 |
| 修改了 `ProjectState` 字段 | 必须同步 `web/lib/types.ts`（snake_case），并在 `model_validator(mode="before")` 处理旧快照兼容 |

---

## 9. 项目结构速览

```text
src/lighting_agent/          # 后端核心
  ├─ web_api.py              # FastAPI 适配层（28+ REST 端点，无业务逻辑）
  ├─ tools.py                # 17 个 LangChain 工具（薄封装）
  ├─ agent.py                # ReAct 编排（LLM 只编排不计算）
  ├─ project_store.py        # ProjectState 版本化持久化 + revision 乐观锁
  ├─ rag.py                  # SQLite / Chroma 双后端证据检索
  ├─ dialux_api.py           # DIALux 目录客户端（缓存/节流/熔断）
  ├─ deliverables.py         # Markdown 报告 + DIALux 交接包 (ZIP)
  ├─ floor_plan.py           # DXF/DWG 平面图解析（shapely 墙线重构）
  └─ calculations/           # 确定性计算（流明法、规则校核）
src/data/user_docs/          # 可入库的资料文档
tests/                       # pytest 测试（全部离线，fake 注入）
web/                         # Next.js 前端工作台
data/                        # SQLite 数据库 + 项目数据（运行时生成）
main.py                      # CLI 入口（离线命令无需 LLM key）
```

## 10. 进阶阅读

- [README.md](../README.md) —— 能力清单、配置项、API 概览
- [照明设计智能体方案](../照明设计智能体方案.md) —— 系统设计
- [DIALux-Luminaire-Finder-API.md](../DIALux-Luminaire-Finder-API.md) —— DIALux 接口细节
- [AGENTS.md](../AGENTS.md) —— 核心不变量（改代码前必读）
- [docs/512会议室-开发记录.md](512会议室-开发记录.md) —— 图纸解析陷阱与开发历史
